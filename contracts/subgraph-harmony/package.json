{
  "name": "spotify-web3-subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ spotify-web3",
    "create-local": "graph create --node http://localhost:8020/ spotify-web3",
    "remove-local": "graph remove --node http://localhost:8020/ spotify-web3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 spotify-web3",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
